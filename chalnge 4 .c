#include <stdio.h>
#include <stdlib.h>

long long factorielle (int n)
{
    if (n==0){
        return 1 ;
    }else {
        return n *factorielle (n-1);
    {
    }
    int main ()
    {
     int nombre ;
     printf("entrer un nombre entier positife :");
     scanf("%f",&nombre);
     if (nombre < 0){
        printf("la factorielle n'est pas definie pour les nombres negative :");
     else {
        printf("la factorielle n'est pas definie pour les nombres negative :") ;
     }
    } 
    return 0 ;
 }